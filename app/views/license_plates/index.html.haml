.d-flex.align-items-center.p-3.my-3.text-white.bg-purple.rounded.shadow-sm
  .lh-2
    %h2.mb-0.text-white.lh-1 Placas
    %small Todas as Placas cadastradas
.my-3.p-3.bg-body.rounded.shadow-sm
  .pull-right
    %a.btn.btn-default-btn-xs.btn-success
      %i.glyphicon.glyphicon-plus
      Novo Registro
  %table.table.table-bordered.table-condensed.table-hover
    %thead
      %tr
        %td ID
        %th Proprietário
        %th Modelo
        %th Ano
        %th Ações
    %tbody#form-list-client-body
    - @license_plates.each do |license_plate|
      %tr{:id => "license_plate-#{license_plate.id}"}
        %td= license_plate.id
        %td= license_plate.owner
        %td= license_plate.model
        %td= license_plate.year
        %td
          %a a
          -# = link_to '', edit_license_plate_path(license_plate), class: "ls-ico-pencil"
          -# = link_to '', license_plate_path(license_plate), method: :delete, class: "ls-ico-remove", |
          -#   data: { confirm: "Tem certeza de que deseja deletar #{license_plate.name}?"}        |
